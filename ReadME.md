This code takes data from csv files and cleans it by removing whitespaces, replacing missing values, deleting rows with impossible values (negative) values, and standardizing column names.

You can run the code in VS Studio Code by clicking the run button on the top right 